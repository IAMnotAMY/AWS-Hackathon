{
  "name": "floorspace-3d-viewer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../infrastructure && npm install",
    "build:all": "cd frontend && npm run build && cd ../backend && npm run build && cd ../infrastructure && npm run build",
    "test:all": "cd frontend && npm test && cd ../backend && npm test && cd ../infrastructure && npm test",
    "lint:all": "cd frontend && npm run lint && cd ../backend && npm run lint"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
